<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sjwlib">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".widget.cityselect.CitySelectActivity" />
        <activity android:name=".widget.touchimageview.ShowImageActivity" />
        <activity android:name=".widget.imageupload.ImageuploadActivity" />
        <activity android:name=".widget.versionupgrade.DownLoadActivity" />
    </application>

</manifest>